<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.areebamansoor.enroutetogether">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_GPS" />
    <uses-permission android:name="android.permission.ACCESS_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".AppClass"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Gen_SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Gen_SignupLogin" />
        <activity android:name=".Gen_SignupForm" />
        <activity android:name=".Offer_OfferRide" />
        <activity android:name=".Gen_SignupActivity" />
        <activity android:name=".Gen_LoginForm" />
        <activity android:name=".Gen_HomeActivity" />
        <activity android:name=".Book_DriversList" />
        <activity android:name=".Book_DriverDetails" />
        <activity android:name=".MessageActivity" />
        <!--
         The API key for Google Maps-based APIs is defined as a string resource.
         (See the file "res/values/google_maps_api.xml").
         Note that the API key is linked to the encryption key used to sign the APK.
         You need a different API key for each encryption key, including the release key that is used to
         sign the APK for publishing.
         You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Offer_PassengersList" />
        <activity android:name=".Offer_PassengerDetails" />
        <activity android:name=".Book_FareEstimate" />
        <activity
            android:name=".ManageProfile"
            android:label="Manage Profile" />
        <activity android:name=".Offer_BillActivity" />
        <activity android:name=".HelpActivity" />
        <activity
            android:name=".BookRideActivity"
            android:label="Book Ride"
            android:screenOrientation="portrait" />
        <activity
            android:name=".OfferRideActivity"
            android:label="Offer Ride"
            android:screenOrientation="portrait" />
        <activity
            android:name=".AddVehicleActivity"
            android:label="Add Vehicle"
            android:screenOrientation="portrait" />
        <activity
            android:name=".EditVehicleActivity"
            android:label="Edit Vehicle"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ActiveDriversActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".ConfirmRide"></activity>
    </application>

</manifest>